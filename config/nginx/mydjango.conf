upstream web {
  ip_hash;
  server web:8000;
}

# portal
server {   
  listen 8000;
  server_name stg-historical-bassoons.com;

  location / {
      proxy_pass stg-historical-bassoons.com;
  }

  location /static/ {    
    autoindex on;
    alias /src/static/; 
  }
  
}
